<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PokeDex</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            padding-top: 20px;
        }
        .pokemon-card {
            margin-bottom: 15px;
            text-align: center;
        }
        .navbar {
            margin-bottom: 20px;
        }
        .card-header {
            background-color: #f8f9fa;
        }
        .card-body {
            background-color: #ffffff;
        }
        .type {
            font-weight: bold;
        }
        .letter-highlight {
            font-size: 2em;
            font-weight: bold;
            margin-bottom: 20px;
        }
        .type-badge {
            padding: 10px;
            border-radius: 5px;
            color: #fff;
            font-weight: bold;
            text-transform: capitalize;
            margin-bottom: 20px;
        }
        .card-title a {
            text-transform: capitalize; 
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center mb-4">PokeDex 1º Geração</h1>
        <div class="row">
            <% results.forEach(pokemon => { %>
                <div class="col-md-4">
                    <div class="card pokemon-card">
                        <div class="card-body">
                            <h5 class="card-title"><a href="/pokemon/<%= pokemon.name %>"><%= pokemon.name %></a></h5>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
