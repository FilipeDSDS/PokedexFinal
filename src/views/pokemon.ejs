<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes do Pokemon</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            padding-top: 20px;
            background-color: #f0f8ff;
        }
        .pokemon-detail {
            text-align: center;
            padding: 20px;
        }
        .pokemon-image {
            max-width: 250px;
            border: 5px solid #fff;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            margin-bottom: 20px;
        }
        .detail-card {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }
        .type {
            font-weight: bold;
            color: #007bff;
        }
        .type-list {
            list-style-type: none;
            padding: 0;
        }
        .type-list li {
            display: inline;
            margin-right: 10px;
        }
        .type-badge {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 12px;
            color: #fff;
            font-weight: bold;
            text-transform: capitalize;
        }

        .type-fire { background-color: #f08030; }
        .type-water { background-color: #6890f0; }
        .type-grass { background-color: #78c850; }
        .type-electric { background-color: #f8d030; }
        .type-ice { background-color: #98d8d8; }
        .type-fighting { background-color: #c03028; }
        .type-poison { background-color: #a040a0; }
        .type-ground { background-color: #e0c068; }
        .type-flying { background-color: #a890f0; }
        .type-psychic { background-color: #f85888; }
        .type-bug { background-color: #a8b820; }
        .type-rock { background-color: #b8a038; }
        .type-ghost { background-color: #705898; }
        .type-dragon { background-color: #7038f8; }
        .type-dark { background-color: #705848; }
        .type-steel { background-color: #b8b8d0; }
        .type-fairy { background-color: #ee99ac; }
        .type-normal { background-color: #a8a878; }
        .table-moves {
            margin-top: 20px;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
        .pokemon-name-uppercase {
            text-transform: capitalize;
        }
    </style>
</head>
<body>
    <div class="container pokemon-detail">
        <div class="detail-card">
            <h1 class="mb-4 pokemon-name-uppercase">Detalhes do Pokemon: <%= name %></h1>
            <img src="<%= pokemon.sprites.other['home'].front_default %>" alt="<%= name %>" class="pokemon-image mb-3">
            <p>ID: <%= pokemon.id %></p>
            <p>Altura: <%= pokemon.height * 10 %> centímetros</p>
            <p>Peso: <%= pokemon.weight / 10 %> quilos</p>
            <h4>Tipo</h4>
            <ul class="type-list">
                <% pokemon.types.forEach(type => { %>
                    <li class="type-badge type-<%= type.type.name %>"><%= type.type.name %></li>
                <% }) %>
            </ul>
            <h3 class="mt-4">Movimentos</h3>
            <table class="table table-bordered table-moves">
                <tbody>
                    <% for (let i = 0; i < pokemon.moves.length; i += 4) { %>
                        <tr>
                            <% for (let j = 0; j < 4; j++) { %>
                                <% if (i + j < pokemon.moves.length) { %>
                                    <td><%= pokemon.moves[i + j].move.name %></td>
                                <% } else { %>
                                    <td></td>
                                <% } %>
                            <% } %>
                        </tr>
                    <% } %>
                </tbody>
            </table>
            <a href="/" class="btn btn-primary mt-4">Voltar para a lista</a>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
